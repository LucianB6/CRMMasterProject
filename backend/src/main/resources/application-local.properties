# Local defaults.
app.prediction.base-url=http://localhost:8001
app.cors.allowed-origins=http://localhost:3000
app.prediction.api-key=
app.prediction.api-key-header=X-API-Key
app.openai.api-key=${OPENAI_API_KEY:}
app.openai.base-url=https://api.openai.com/v1
app.openai.chat-model=gpt-4o-mini
app.openai.embedding-model=text-embedding-3-small
app.openai.vector-store-id=${git OPENAI_VECTOR_STORE_ID:}
app.openai.vector-search-max-results=${OPENAI_VECTOR_SEARCH_MAX_RESULTS:5}
app.auth.reset-password-base-url=${APP_AUTH_RESET_PASSWORD_BASE_URL:http://localhost:3000/reset-password}
app.email.enabled=${APP_EMAIL_ENABLED:true}
app.email.log-only=${APP_EMAIL_LOG_ONLY:false}
app.email.invitation-subject=${APP_EMAIL_INVITATION_SUBJECT:Invitation to join your company workspace}
app.email.password-reset-subject=${APP_EMAIL_PASSWORD_RESET_SUBJECT:Reset your password}
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:lucianbaciu4@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:ibnc ucqy tsjw lfkg}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_SMTP_STARTTLS_ENABLE:true}
